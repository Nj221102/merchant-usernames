# Greenlight Lightning Backend Configuration
# Copy this file to .env and update with your actual values

# Database Configuration
# Replace with your PostgreSQL connection details
DATABASE_URL=postgresql://username:password@localhost:5432/greenlight_wallet

# JWT Configuration
# Generate a secure secret: openssl rand -base64 64
JWT_SECRET=your-super-secret-jwt-key-change-in-production-must-be-at-least-32-chars

# Server Configuration
HOST=0.0.0.0
PORT=8080

# Greenlight Credentials
# Option 1: Use file paths (recommended for development)
GL_CERT_PATH=./client.crt
GL_KEY_PATH=./client-key.pem

# Option 2: Use content directly (alternative for deployment)
# GL_CERT_CONTENT=-----BEGIN CERTIFICATE-----...
# GL_KEY_CONTENT=-----BEGIN PRIVATE KEY-----...

# Network Configuration
# Use 'bitcoin' for mainnet, 'testnet' for testnet
GL_NETWORK=bitcoin

# Logging Configuration
# Options: error, warn, info, debug, trace
RUST_LOG=greenlight_backend=info,tower_http=info

# Development/Debug Settings (uncomment for debugging)
# RUST_LOG=greenlight_backend=debug,tower_http=debug,sqlx=debug
# RUST_BACKTRACE=1

# Production Settings
# Uncomment and configure for production deployment
# RUST_LOG=greenlight_backend=warn,error
# DATABASE_MAX_CONNECTIONS=20
# JWT_EXPIRY_HOURS=24
